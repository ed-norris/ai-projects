import SwiftUI

// Kept as a non-entry type to avoid duplicate @main when using Xcode template app file.
struct MetronomeiOSRootView: View {
    var body: some View {
        iOSContentView()
    }
}
